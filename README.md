# 智人排行榜 (astrbot_plugin_zhiren_leaderboard)

群聊记分 + 排行榜 AstrBot 插件。

## 功能

- **记分**：群里发 `@某成员 加一分 理由` → 该成员 +1 分；`@某成员 扣一分 理由` → -1 分（`减一分` 同义）。必须附理由，不能给自己记分，10 秒内重复消息自动去重。
- **成员信息库**：所有发言成员昵称实时写入 SQLite（`plugin_data/zhiren_leaderboard/zhiren.db`），@目标自动拉取群名片更新。
- **查询**（@机器人）：
  - `总分榜` / `总分` —— 总分排行榜
  - `周榜` / `本周排行` —— 本周排行榜
  - `查询 @某人`（或 `查询 名字`）—— 个人得分详情（总分、本周、最近 20 条记录）
  - `我的得分` —— 查自己
- **每周播报**：每周一 00:05 自动向有记录的群播报上一周排行榜。
- **AI 裁判**（v1.1.0 新增）：管理员发送 `@机器人 AI评分 开` 启用。开启后群里非 @bot 的发言进入上下文缓冲（最近 60 条），每 4 分钟 AI 开庭一次，依据以下标准判罚 ±3 分并公示：
  1. **德行不足**（放鸽子、爽约、甩锅等引起公愤的行为）：-1~-3
  2. **满嘴脏话**（高门槛：连续刷脏话或大段辱骂才判）：-2~-3
  3. **下头性暗示**：-1~-3
  4. **智商掉线**（迷惑发言且明显引发群友嘲笑）：-1~-2
  5. **其他逆天**：自由裁量 -1~-3
  6. 极少数惊艳金句可 +1

  判罚综合当事人连续发言与其他群友的反应（嘲笑、声讨）作为证据。`@机器人 评价 @某人` 随时专项裁决；`@机器人 AI评分 状态` 查看状态。需要已配置 LLM Provider。
- **Web 界面**：插件自带 HTTP 服务（默认 `6201` 端口，可用环境变量 `ZHIREN_WEB_PORT` 覆盖），浏览器打开 `http://<服务器IP>:<端口>` 查看：本周榜/总分榜切换、前三名领奖台、成员搜索、点击查看个人详情，30 秒自动刷新。当前服务器部署使用 `ZHIREN_WEB_PORT=6180`（该端口已在阿里云安全组放行）。

## 配置

- Web 端口：环境变量 `ZHIREN_WEB_PORT`（默认 6201）。
- 记分关键词固定为「加一分 / 扣一分 / 减一分」。

## 文件

- `main.py` 插件主体（事件处理、数据库、Web 服务、周榜定时任务）
- `zhiren_logic.py` 纯逻辑（指令解析、周键计算，无 astrbot 依赖）
- `web/index.html` 排行榜页面（单文件、无外部依赖）
